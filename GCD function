#include <iostream>
using namespace std;
int GCD (int a, int b){
    if ( b==0 ) {
       return 0;
    }
    else{
        return GCD (b,a%b);
    }
}
int main(){
  int ars = GCD (12,18);
  cout<<ars;
}
